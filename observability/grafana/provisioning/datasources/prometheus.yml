# Auto-provisions Prometheus as Grafana's default datasource on startup (Phase 18a).
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
